version: '3.8'

services:
  backend:
    build: .
    container_name: medicas-bff
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:oracle:thin:@alertasdb_high?TNS_ADMIN=/app/wallet
      - SPRING_DATASOURCE_USERNAME=ADMIN
      - SPRING_DATASOURCE_PASSWORD=Alertas4569@
    volumes:
      - C:/Medicas/Wallet_AlertasDB:/app/wallet
    restart: unless-stopped
